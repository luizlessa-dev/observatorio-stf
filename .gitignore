node_modules/
dist/
.env
.env.local
*.log
.DS_Store
__pycache__/
*.pyc
.venv/
.vercel

# Claude Code — configuração local de sessão (não versionar)
.claude/

# Tipos gerados pelo Astro
.astro/

# og:image geradas em build time por scripts/gerar-og-images.mjs — refeitas
# a cada build, não versionadas (ver package.json "build").
public/og/
